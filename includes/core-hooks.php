<?php
if ( ! defined( 'ABSPATH' ) ) exit; // Exit if accessed directly

/**
 * Define a constante de debug para o plugin.
 * Defina como `true` para habilitar logs e painel de debug no frontend.
 */
if ( ! defined( 'CC_DEBUG' ) ) {
    define( 'CC_DEBUG', false ); // Mude para true para debug
} 